{
  "name": "time-calc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cf:login": "npx --yes wrangler login",
    "cf:whoami": "npx --yes wrangler whoami",
    "deploy": "npm run build && npx --yes wrangler pages deploy dist --project-name=work-time-calculator --branch=main --commit-dirty=true",
    "deploy:preview": "npm run build && npx --yes wrangler pages deploy dist --project-name=work-time-calculator --commit-dirty=true"
  },
  "dependencies": {
    "@vueuse/core": "^14.1.0",
    "dayjs": "^1.11.19",
    "vue": "^3.5.25"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.2",
    "vite": "^7.2.4",
    "vite-plugin-vue-devtools": "^8.0.5"
  }
}
